Reworking ui pipeline to reuse texture resources without an instance buffer
3d models will be reworked later with using the instance buffer
(get all blocks -> separate by block type -> construct instance buffers for each block type)